version: '3'

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3080:3080"
    restart: unless-stopped
    volumes:
      - /etc/caddy/Caddyfile:/Caddyfile:ro